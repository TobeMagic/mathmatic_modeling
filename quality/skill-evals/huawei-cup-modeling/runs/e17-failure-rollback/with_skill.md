# Baseline 失败必须书面转向

贪心不可行 → ledger 写 `failed` 和原因，给出 pivot（例如规则策略放宽约束，而不是把 NSGA-II 包装成已完成的 baseline）。

摘要现在不能写该问数字。`status=not-run` 或 `failed` 的格子留空。不要把未覆盖硬要求的升级算法写成 baseline。

```text
AWAITING_HUMAN_REVIEW(baseline)
```
