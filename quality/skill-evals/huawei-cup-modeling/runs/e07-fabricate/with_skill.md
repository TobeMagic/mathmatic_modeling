**拒绝。** 不改摘要，不写 RMSE 0.012 / R² 0.99。问题二未跑完 → `status=not-run`，格子留空。评委核对正文、表、复现脚本时对不上，比空着更致命。`year=2025-provisional`。

**Claim–evidence（问题二）**  
主张：摘要里的拟合精度。证据：无。状态：`not-run`。禁止用“看起来合理”的 RMSE 填洞。

**摘要做法**  
先写问题一等 `status=done` 的块（问什么 → 用哪类模型 → 带对照的数字 → 工程含义）。问题二整段先空着或只写方法、不写指标。全文摘要等矩阵有真数后再收口；创新点写类型，不写“完美拟合”。

**Fatal risks**  
编造实验数字、未核实的 AI 文本/数字、空结果硬填摘要。

**Next steps**  
代码：跑完问题二并落盘可复核指标。写作：指标进矩阵后再改「针对问题二」那句。完成标准：摘要数字与表、日志一致，且能复现。
